{
  CreateVacationFunction function=new CreateVacationFunction(name,Temperature.fromLocalTemperature(coolHoldTemp.toBigDecimal()),Temperature.fromLocalTemperature(heatHoldTemp.toBigDecimal()),startDateTime,endDateTime,FanMode.forValue(fan),fanMinOnTime);
  return callEcobeeInternal(item,function);
}
