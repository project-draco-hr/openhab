{
  if (data[pos - 1] == (byte)0xA9) {
    if (data[pos] == (byte)0x00) {
      return (byte)0xA9;
    }
 else     if (data[pos] == (byte)0x01) {
      return (byte)0xAA;
    }
  }
  return data[pos];
}
