{
  for (  C networkChannel : channelTrackers.keySet()) {
    if (channelTrackers.get(networkChannel).host.equals(socketAddress.getHostName()) && channelTrackers.get(networkChannel).port == socketAddress.getPort()) {
      return channelTrackers.get(networkChannel).referers.size();
    }
  }
  return 0;
}
