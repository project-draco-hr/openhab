{
  String newCommand=config.transformCommand(command);
  if (newCommand != null) {
    invokeScene(config);
  }
 else {
    logger.warn("invokeScene: Command '{}' not supported, or missing command: declaration, for Item '{}'",command.toString(),config.getItemName());
  }
}
