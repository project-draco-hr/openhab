{
  short oldCurrentPosition=currentPosition;
  currentPosition=newCurrentPosition;
  if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,ModelPackage.MSERVO__CURRENT_POSITION,oldCurrentPosition,currentPosition));
}
