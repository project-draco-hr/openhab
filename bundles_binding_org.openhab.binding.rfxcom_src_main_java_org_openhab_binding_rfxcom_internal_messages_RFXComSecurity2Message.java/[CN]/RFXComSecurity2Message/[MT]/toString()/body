{
  String str="";
  str+=super.toString();
  str+="\n - Sub type = " + subType;
  str+="\n - Id = " + generateDeviceId();
  str+="\n - Button Status = " + buttonStatus;
  str+="\n - Battery level = " + batteryLevel;
  str+="\n - Signal level = " + signalLevel;
  return str;
}
