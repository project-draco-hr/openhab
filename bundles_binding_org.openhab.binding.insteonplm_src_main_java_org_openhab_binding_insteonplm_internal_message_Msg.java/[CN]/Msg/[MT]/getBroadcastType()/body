{
  if (m_definition == null || !m_definition.containsField("msgType"))   return MsgType.INVALID;
  try {
    return MsgType.s_fromValue(getByte("msgType"));
  }
 catch (  FieldException e) {
  }
  return MsgType.INVALID;
}
