{
  if (config.indexOf(':') != -1) {
    return config.substring(0,config.indexOf(":"));
  }
  if (config.indexOf("[") != -1) {
    return config.substring(0,config.indexOf("["));
  }
  return config;
}
