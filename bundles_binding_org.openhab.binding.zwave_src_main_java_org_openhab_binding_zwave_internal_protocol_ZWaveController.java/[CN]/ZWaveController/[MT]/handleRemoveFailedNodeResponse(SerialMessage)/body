{
  logger.debug("Got RemoveFailedNode response.");
  if (incomingMessage.getMessagePayloadByte(0) == 0x00) {
    logger.debug("Remove failed node successfully placed on stack.");
  }
 else   logger.error("Remove failed node not placed on stack due to error 0x{}.",Integer.toHexString(incomingMessage.getMessagePayloadByte(0)));
}
