{
  logger.debug("Got RemoveFailedNode response.");
  if (incomingMessage.getMessageBuffer()[2] != 0x00)   logger.debug("Remove failed node successfully placed on stack.");
 else   logger.error("Remove failed node not placed on stack due to error.");
}
