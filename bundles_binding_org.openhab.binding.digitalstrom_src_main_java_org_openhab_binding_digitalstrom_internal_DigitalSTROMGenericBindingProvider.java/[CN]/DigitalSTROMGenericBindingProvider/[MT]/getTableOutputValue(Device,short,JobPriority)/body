{
switch (priority) {
case URGENT:
    addJob(new GetSceneOutputValue(device,sceneId));
  break;
case NO_HURRY:
addNextTimeJob(new GetSceneOutputValue(device,sceneId));
break;
default :
addJob(new GetSceneOutputValue(device,sceneId));
}
}
