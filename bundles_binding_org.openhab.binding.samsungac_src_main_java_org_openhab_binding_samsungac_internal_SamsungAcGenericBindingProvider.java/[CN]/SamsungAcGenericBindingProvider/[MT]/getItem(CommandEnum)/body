{
  for (  BindingConfig config : bindingConfigs.values()) {
    SamsungAcBindingConfig con=(SamsungAcBindingConfig)config;
    if (property.equals(con.getProperty()))     return con;
  }
  return null;
}
