{
  if (type.equals("IN"))   return BindingType.IN;
  if (type.equals("OUT"))   return BindingType.OUT;
  if (type.equals("WATCHDOG"))   return BindingType.WATCHDOG;
  throw new RuntimeException("Invalid binding type: " + type + " (only support IN/OUT/WATCHDOG)");
}
