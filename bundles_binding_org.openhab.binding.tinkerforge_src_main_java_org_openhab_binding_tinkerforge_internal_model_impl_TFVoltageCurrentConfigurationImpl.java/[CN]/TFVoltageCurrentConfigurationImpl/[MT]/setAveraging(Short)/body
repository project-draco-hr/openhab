{
  Short oldAveraging=averaging;
  averaging=newAveraging;
  if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,ModelPackage.TF_VOLTAGE_CURRENT_CONFIGURATION__AVERAGING,oldAveraging,averaging));
}
