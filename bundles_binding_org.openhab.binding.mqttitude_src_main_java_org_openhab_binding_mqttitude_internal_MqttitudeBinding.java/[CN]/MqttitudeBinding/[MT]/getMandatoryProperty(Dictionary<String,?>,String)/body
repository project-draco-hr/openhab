{
  String value=(String)properties.get(name);
  if (StringUtils.isBlank(value))   throw new ConfigurationException("mqttitude:" + name,"Missing or invalid property '" + name + "'");
  return value.trim();
}
