{
  String str="";
  str+=super.toString();
  str+="\n - Sub type = " + subType;
  str+="\n - Id = " + sensorId;
  str+="\n - Command = " + command + "("+ commandId+ ")";
  str+="\n - Pulse = " + pulse;
  return str;
}
