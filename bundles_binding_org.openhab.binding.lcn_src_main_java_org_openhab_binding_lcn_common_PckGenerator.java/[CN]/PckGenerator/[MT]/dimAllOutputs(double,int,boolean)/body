{
  int n=(int)Math.round(percent * 2);
  if (is1805) {
    return String.format("OY%03d%03d%03d%03d%03d",n,n,n,n,ramp);
  }
  if (n == 0) {
    return String.format("AA%03d",ramp);
  }
 else   if (n == 200) {
    return String.format("AE%03d",ramp);
  }
  return String.format("AH%03d",n / 2);
}
