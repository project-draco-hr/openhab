{
  if (StringUtils.isBlank(ObjectUtils.toString(value))) {
    logger.warn("{} for datapoint {} for device {} is empty!",fieldName,dp.getName(),dp.getChannel().getDevice().getAddress());
  }
 else {
    writeField(dp,fieldName,value,value.getClass());
  }
}
