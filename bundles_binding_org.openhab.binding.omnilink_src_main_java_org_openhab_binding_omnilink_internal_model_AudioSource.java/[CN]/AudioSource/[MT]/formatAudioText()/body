{
  StringBuilder sb=new StringBuilder();
  for (  String s : audioText) {
    if (sb.length() > 0)     sb.append(" | ");
    sb.append(s);
  }
  if (sb.length() == 0)   sb.append(" ");
  return sb.toString();
}
