{
  try {
    if (clientSocket != null) {
      clientSocket.close();
    }
  }
 catch (  IOException e) {
    logger.error("Failed to disconnect from SqueezeServer at " + host + ":"+ cliPort,e);
  }
 finally {
    clientSocket=null;
  }
}
