{
  super();
  this.fadeTime=fadeTime;
  this.targetValue=DmxUtil.capDmxValue(targetValue);
  this.holdTime=holdTime;
  if (holdTime < -1) {
    this.holdTime=-1;
  }
  if (fadeTime < 0) {
    this.fadeTime=0;
  }
}
