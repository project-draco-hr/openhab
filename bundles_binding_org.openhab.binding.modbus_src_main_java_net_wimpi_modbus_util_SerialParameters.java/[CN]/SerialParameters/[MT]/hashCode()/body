{
  return new HashCodeBuilder(7,51).append(m_PortName).append(m_BaudRate).append(m_FlowControlIn).append(m_FlowControlOut).append(m_Databits).append(m_Stopbits).append(m_Parity).append(m_Encoding).append(m_Echo).append(m_ReceiveTimeoutMillis).toHashCode();
}
