{
  new TellstickActivator().start(null);
  new TellstickBinding().activate();
  while (true) {
    try {
      Thread.sleep(500);
    }
 catch (    InterruptedException e) {
      e.printStackTrace();
    }
  }
}
