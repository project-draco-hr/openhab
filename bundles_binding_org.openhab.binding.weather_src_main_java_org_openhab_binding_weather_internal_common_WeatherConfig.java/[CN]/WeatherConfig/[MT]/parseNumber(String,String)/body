{
  try {
    return Double.parseDouble(value);
  }
 catch (  Exception ex) {
    throw new ConfigurationException("weather","Parameter '" + key + "' empty or in wrong format, please check your openhab.cfg!");
  }
}
