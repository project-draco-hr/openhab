{
  ProviderName providerName=ProviderName.parse(name);
  if (providerName == null) {
    throw new ConfigurationException("weather","Provider with name '" + name + "' not found, please check your openhab.cfg!");
  }
  return providerName;
}
