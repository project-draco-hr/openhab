{
  if (valueList != null) {
    for (int i=0; i < valueList.length; i++) {
      String value=valueList[i];
      if (valueListValue.equalsIgnoreCase(value)) {
        return i;
      }
    }
  }
  return -1;
}
