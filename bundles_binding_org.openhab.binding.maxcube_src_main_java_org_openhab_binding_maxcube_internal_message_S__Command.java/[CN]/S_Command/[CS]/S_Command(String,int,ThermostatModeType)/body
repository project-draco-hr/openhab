{
  this.rfAddress=rfAddress;
  this.roomId=roomId;
switch (mode) {
case VACATION:
case MANUAL:
    break;
case AUTOMATIC:
  bits=Utils.getBits(0);
break;
case BOOST:
bits=Utils.getBits(255);
break;
default :
}
}
