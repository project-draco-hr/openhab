{
  try {
    logger.debug("Sending command: " + value + " to property:"+ property+ " with ip:"+ aircon.getIpAddress());
    aircon.sendCommand(property,value);
  }
 catch (  Exception e) {
    logger.warn("Could not send value: '" + value + "' to property:'"+ property+ "'");
  }
}
