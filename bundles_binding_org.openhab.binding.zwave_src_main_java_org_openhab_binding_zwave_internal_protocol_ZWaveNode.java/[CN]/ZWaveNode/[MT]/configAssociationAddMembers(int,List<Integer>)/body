{
  AssociationGroup association=configAssociations.get(group);
  if (association == null)   association=new AssociationGroup();
  association.Nodes.addAll(members);
  configAssociations.put(group,association);
}
