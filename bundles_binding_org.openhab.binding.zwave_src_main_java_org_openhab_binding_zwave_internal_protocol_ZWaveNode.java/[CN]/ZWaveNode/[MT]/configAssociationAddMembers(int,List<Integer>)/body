{
  NodeAssociation association=configAssociations.get(group);
  if (association == null)   association=new NodeAssociation();
  association.Nodes.addAll(members);
  configAssociations.put(group,association);
}
