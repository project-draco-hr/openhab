{
  if (registerId < 0 || registerId >= thrsToVarArrays.length || thrsId < 0 || thrsId >= (registerId == 0 ? 5 : 4)) {
    throw new IllegalArgumentException();
  }
  return thrsToVarArrays[registerId][thrsId];
}
