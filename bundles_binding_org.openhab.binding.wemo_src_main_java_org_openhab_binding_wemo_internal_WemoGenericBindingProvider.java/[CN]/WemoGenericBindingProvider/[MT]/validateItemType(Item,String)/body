{
  if (!(item instanceof ContactItem || item instanceof NumberItem || item instanceof SwitchItem)) {
    throw new BindingConfigParseException("item '" + item.getName() + "' is of type '"+ item.getClass().getSimpleName()+ "', only Contact-, Number- and SwitchItems are allowed - please check your *.items configuration");
  }
}
