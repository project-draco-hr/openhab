{
  logger.debug("{} EnumerateListener was called.",LoggerConstants.TFINIT);
  if (enumerationType == IPConnection.ENUMERATION_TYPE_DISCONNECTED)   removeDevice(uid);
 else   addDevice(uid,connectedUid,deviceIdentifier);
}
