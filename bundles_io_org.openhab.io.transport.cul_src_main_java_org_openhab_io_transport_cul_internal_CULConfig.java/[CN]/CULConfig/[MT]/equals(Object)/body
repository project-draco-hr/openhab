{
  if (this == obj) {
    return true;
  }
  if (obj == null) {
    return false;
  }
  if (getClass() != obj.getClass()) {
    return false;
  }
  CULConfig other=(CULConfig)obj;
  return Objects.equals(deviceAddress,other.deviceAddress) && Objects.equals(deviceType,other.deviceType) && mode == other.mode;
}
