{
  ObjectMapper mapper=new ObjectMapper();
  try {
    settingsData=new SettingsTree(mapper.readValue(settings,Map.class));
  }
 catch (  Exception e) {
    logger.error("Could not read Settings-Json from Hue Bridge.");
  }
}
