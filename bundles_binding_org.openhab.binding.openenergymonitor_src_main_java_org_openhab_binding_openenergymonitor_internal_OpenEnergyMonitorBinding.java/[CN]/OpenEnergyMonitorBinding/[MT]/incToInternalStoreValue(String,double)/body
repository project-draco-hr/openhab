{
  double result;
  try {
    result=valueStore.incValue(itemName,value);
  }
 catch (  IllegalArgumentException e) {
    initializeItem(itemName);
    result=valueStore.incValue(itemName,value);
  }
  return result;
}
