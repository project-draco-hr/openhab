{
  for (  Set<Item> lvItems : contextMap.values()) {
    if (lvItems != null) {
      for (      Item lvItem : lvItems) {
        if (pvItemName.equals(lvItem.getName())) {
          return lvItem;
        }
      }
    }
  }
  return null;
}
