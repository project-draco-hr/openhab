{
  final int prime=31;
  int result=1;
  result=prime * result + ((mode == null) ? 0 : mode.hashCode());
  result=prime * result + ((sceneNumber == null) ? 0 : sceneNumber.hashCode());
  return result;
}
