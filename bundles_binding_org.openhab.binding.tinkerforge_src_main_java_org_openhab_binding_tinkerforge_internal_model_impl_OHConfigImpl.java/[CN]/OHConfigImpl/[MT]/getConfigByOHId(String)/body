{
  EList<OHTFDevice<?>> _ohTfDevices=getOhTfDevices();
  for (  final OHTFDevice<?> ohTfDevice : _ohTfDevices) {
    String _ohid=ohTfDevice.getOhid();
    if (_ohid.equals(ohid))     return ohTfDevice;
  }
  return null;
}
