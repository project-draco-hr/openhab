{
  String str="";
  if (rawMessage != null) {
    str+=super.toString();
  }
  str+="\n - Sub type = " + subType;
  str+="\n - Id = " + id1 + " "+ id2+ " "+ id3;
  str+="\n - Unit code = " + unitCode;
  str+="\n - Command = " + command;
  str+="\n - Signal level = " + signalLevel;
  return str;
}
