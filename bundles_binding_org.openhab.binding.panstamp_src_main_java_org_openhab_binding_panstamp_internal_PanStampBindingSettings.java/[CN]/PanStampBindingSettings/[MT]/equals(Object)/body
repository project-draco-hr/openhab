{
  if (obj instanceof PanStampBindingSettings) {
    PanStampBindingSettings other=(PanStampBindingSettings)obj;
    return !serialDiffers(other) && !networkDiffers(other) && !debugDiffers(other)&& !directoriesDiffers(other);
  }
  return false;
}
