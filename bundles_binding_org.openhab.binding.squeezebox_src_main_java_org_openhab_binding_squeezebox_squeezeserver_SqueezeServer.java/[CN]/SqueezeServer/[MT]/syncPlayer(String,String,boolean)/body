{
  String id=getPlayerIdByOhName(ohName);
  if ((!mac.equals("")) && (!id.equals(""))) {
    if (true == remove) {
      sendMsg(id + " sync -");
    }
 else {
      sendMsg(id + " sync " + mac);
    }
  }
}
