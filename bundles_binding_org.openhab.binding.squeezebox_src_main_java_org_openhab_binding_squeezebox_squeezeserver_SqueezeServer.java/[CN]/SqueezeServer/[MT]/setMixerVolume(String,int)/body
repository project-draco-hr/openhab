{
  String id=getPlayerIdByOhName(ohName);
  if (!id.equals("")) {
    if (0 > volume) {
      volume=0;
    }
 else     if (100 < volume) {
      volume=100;
    }
    sendMsg(id + " mixer volume " + String.valueOf(volume));
  }
}
