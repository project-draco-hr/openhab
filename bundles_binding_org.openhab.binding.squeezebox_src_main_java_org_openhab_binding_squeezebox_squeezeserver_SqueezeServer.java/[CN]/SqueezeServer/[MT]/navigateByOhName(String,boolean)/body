{
  String id=getPlayerIdByOhName(ohName);
  if (!id.equals("")) {
    if (true == prev) {
      sendMsg(id + " playlist index -1");
    }
 else {
      sendMsg(id + " playlist index +1");
    }
  }
}
