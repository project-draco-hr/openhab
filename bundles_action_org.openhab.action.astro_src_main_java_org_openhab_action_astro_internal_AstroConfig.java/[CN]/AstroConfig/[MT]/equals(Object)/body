{
  if (obj == null || !(obj instanceof AstroConfig)) {
    return false;
  }
  AstroConfig config=(AstroConfig)obj;
  return date.getTime() == config.getDate().getTime() && latitude == config.getLatitude() && longitude == config.getLongitude();
}
