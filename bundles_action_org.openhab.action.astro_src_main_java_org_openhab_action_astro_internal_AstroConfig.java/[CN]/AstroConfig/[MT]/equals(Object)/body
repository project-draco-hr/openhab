{
  if (obj == null || !(obj instanceof AstroConfig)) {
    return false;
  }
  AstroConfig config=(AstroConfig)obj;
  return new EqualsBuilder().append(date,config.getDate()).append(latitude,config.getLatitude()).append(longitude,config.getLongitude()).isEquals();
}
