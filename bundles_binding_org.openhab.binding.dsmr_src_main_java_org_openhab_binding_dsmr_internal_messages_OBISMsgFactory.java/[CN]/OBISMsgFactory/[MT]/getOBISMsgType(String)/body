{
  if (obisId.length() > 0) {
    for (    OBISMsgType t : OBISMsgType.values()) {
      if (t.obisReference.equals(obisId) && t.applicableVersions.contains(version)) {
        return t;
      }
    }
  }
  return OBISMsgType.UNKNOWN;
}
