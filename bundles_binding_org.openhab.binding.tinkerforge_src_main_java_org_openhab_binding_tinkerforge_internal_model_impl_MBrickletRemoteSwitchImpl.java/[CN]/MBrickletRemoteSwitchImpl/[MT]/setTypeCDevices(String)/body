{
  String oldTypeCDevices=typeCDevices;
  typeCDevices=newTypeCDevices;
  if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,ModelPackage.MBRICKLET_REMOTE_SWITCH__TYPE_CDEVICES,oldTypeCDevices,typeCDevices));
}
