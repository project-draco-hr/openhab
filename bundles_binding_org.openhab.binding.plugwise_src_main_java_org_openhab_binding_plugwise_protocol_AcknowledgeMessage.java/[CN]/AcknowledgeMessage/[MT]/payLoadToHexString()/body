{
switch (code) {
case CIRCLEPLUS:
    return String.format("%04X",code.toInt()) + extendedMAC;
case ON:
  return String.format("%04X",code.toInt()) + extendedMAC;
case OFF:
return String.format("%04X",code.toInt()) + extendedMAC;
default :
return String.format("%04X",code.toInt());
}
}
