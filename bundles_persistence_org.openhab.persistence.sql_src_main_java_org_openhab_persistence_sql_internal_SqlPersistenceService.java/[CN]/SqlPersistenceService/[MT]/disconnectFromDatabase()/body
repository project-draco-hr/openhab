{
  if (isConnected()) {
    try {
      connection.close();
      logger.debug("Disconnected from database " + url);
      connection=null;
    }
 catch (    Exception e) {
      logger.warn("Failed disconnecting from the SQL database",e);
    }
  }
}
