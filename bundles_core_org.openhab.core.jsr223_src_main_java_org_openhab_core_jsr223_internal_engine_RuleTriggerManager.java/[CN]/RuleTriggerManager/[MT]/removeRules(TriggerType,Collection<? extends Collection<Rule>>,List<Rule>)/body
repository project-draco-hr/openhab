{
  for (  Collection<Rule> ruleSet : ruleSets) {
    for (    Rule rule : rules) {
      ruleSet.remove(rule);
      if (type == TriggerType.TIMER) {
        removeTimerRule(rule);
      }
    }
  }
}
