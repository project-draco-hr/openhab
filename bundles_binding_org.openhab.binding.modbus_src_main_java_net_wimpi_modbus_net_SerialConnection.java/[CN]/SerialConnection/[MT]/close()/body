{
  if (!m_Open) {
    return;
  }
  if (m_SerialPort != null) {
    try {
      m_Transport.close();
      m_SerialIn.close();
    }
 catch (    IOException e) {
      logger.error(e.getMessage());
    }
    m_SerialPort.close();
  }
  m_Open=false;
}
