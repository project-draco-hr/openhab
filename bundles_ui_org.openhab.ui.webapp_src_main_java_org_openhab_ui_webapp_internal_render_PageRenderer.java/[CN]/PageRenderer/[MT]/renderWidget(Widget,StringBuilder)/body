{
  if (itemUIRegistry.getVisiblity(w) == false)   return null;
  for (  WidgetRenderer renderer : widgetRenderers) {
    if (renderer.canRender(w)) {
      return renderer.renderWidget(w,sb);
    }
  }
  return null;
}
