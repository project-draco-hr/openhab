{
  updateAssociationsNode=1;
  configAssociations.clear();
  SerialMessage serialMessage=getAssociationMessage(updateAssociationsNode);
  if (serialMessage != null)   this.getController().sendData(serialMessage);
}
