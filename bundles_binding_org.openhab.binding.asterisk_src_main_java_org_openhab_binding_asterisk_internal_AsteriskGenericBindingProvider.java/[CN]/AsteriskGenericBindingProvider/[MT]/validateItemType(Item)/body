{
  if (!(item instanceof CallItem || item instanceof SwitchItem)) {
    throw new BindingConfigParseException("item '" + item.getName() + "' is of type '"+ item.getClass().getSimpleName()+ "', only Call- and SwitchItems are allowed - please check your *.items configuration");
  }
}
