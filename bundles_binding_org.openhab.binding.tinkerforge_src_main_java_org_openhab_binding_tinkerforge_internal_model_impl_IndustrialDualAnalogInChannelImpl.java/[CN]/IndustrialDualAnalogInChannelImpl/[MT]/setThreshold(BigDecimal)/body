{
  BigDecimal oldThreshold=threshold;
  threshold=newThreshold;
  if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,ModelPackage.INDUSTRIAL_DUAL_ANALOG_IN_CHANNEL__THRESHOLD,oldThreshold,threshold));
}
