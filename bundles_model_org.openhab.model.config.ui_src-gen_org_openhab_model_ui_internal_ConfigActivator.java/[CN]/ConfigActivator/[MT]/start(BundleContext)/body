{
  super.start(context);
  INSTANCE=this;
  try {
    injectors.put("org.openhab.model.Config",Guice.createInjector(Modules.override(Modules.override(getRuntimeModule("org.openhab.model.Config")).with(getUiModule("org.openhab.model.Config"))).with(getSharedStateModule())));
  }
 catch (  Exception e) {
    Logger.getLogger(getClass()).error(e.getMessage(),e);
    throw e;
  }
}
