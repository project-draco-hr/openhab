{
  if (delay < CONFIG_MIN_STARTUP_DELAY) {
    logger.warn("Startup Delay of {} below minimum permitted, {} used.",startupDelay,CONFIG_MIN_STARTUP_DELAY);
    delay=CONFIG_MIN_STARTUP_DELAY;
  }
  this.startupDelay=delay;
}
