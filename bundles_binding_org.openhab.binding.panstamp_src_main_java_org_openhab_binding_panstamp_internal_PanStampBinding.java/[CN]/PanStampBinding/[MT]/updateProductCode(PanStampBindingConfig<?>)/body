{
  store.addProductCode(conf.getAddress(),conf.getManufacturerId(),conf.getProductId());
  logger.debug("Product code for {} updated to {}/{}",conf.getAddress(),conf.getManufacturerId(),conf.getProductId());
}
