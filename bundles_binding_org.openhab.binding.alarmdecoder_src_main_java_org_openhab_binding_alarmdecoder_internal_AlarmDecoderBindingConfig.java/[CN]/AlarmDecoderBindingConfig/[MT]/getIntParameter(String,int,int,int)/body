{
  int i=deflt;
  String v=m_params.get(key);
  if (v != null) {
    try {
      int tmp=Integer.parseInt(v);
      if (tmp >= min && tmp <= max) {
        i=tmp;
      }
    }
 catch (    NumberFormatException e) {
    }
  }
  return i;
}
