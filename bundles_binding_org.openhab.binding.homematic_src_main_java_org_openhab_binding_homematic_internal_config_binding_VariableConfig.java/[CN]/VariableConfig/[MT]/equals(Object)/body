{
  if (obj == null || !(obj instanceof VariableConfig)) {
    return false;
  }
  VariableConfig comp=(VariableConfig)obj;
  return new EqualsBuilder().append(name,comp.getName()).append(this.getClass().getName(),comp.getClass().getName()).isEquals();
}
