{
  if (obj == null || !(obj instanceof DatapointConfig)) {
    return false;
  }
  DatapointConfig comp=(DatapointConfig)obj;
  return new EqualsBuilder().append(address,comp.getAddress()).append(channel,comp.getChannel()).append(parameter,comp.getParameter()).isEquals();
}
