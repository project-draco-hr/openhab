{
  String str="";
  str+=super.toString();
  str+="\n - Sub type = " + subType;
  str+="\n - Unit code = " + unitcode;
  str+="\n - Command = " + command + "("+ commandId+ ")";
  str+="\n - Signal level = " + signalLevel;
  return str;
}
