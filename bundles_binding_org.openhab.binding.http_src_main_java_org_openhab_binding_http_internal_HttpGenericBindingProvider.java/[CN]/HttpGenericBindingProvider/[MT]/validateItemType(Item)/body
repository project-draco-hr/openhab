{
  if (item instanceof DimmerItem || item instanceof RollershutterItem) {
    throw new BindingConfigParseException("item '" + item.getName() + "' is of type '"+ item.getClass().getSimpleName()+ "', only Switch- and String-, Number-, and ContanctItems are allowed - please check your *.items configuration");
  }
}
