{
  try {
    final String url=prepare();
    final String json=executeQuery(url);
    final DeviceListResponse response=JSON.readValue(json,DeviceListResponse.class);
    return response;
  }
 catch (  final Exception e) {
    throw new NetatmoException("Could not execute device list request!",e);
  }
}
