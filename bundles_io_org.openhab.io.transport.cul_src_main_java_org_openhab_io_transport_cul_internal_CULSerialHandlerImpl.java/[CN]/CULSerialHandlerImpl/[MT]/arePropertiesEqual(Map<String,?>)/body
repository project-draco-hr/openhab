{
  if (properties == null) {
    return false;
  }
  boolean baudRateEquals=mapContainsEqualValueByKey(properties,KEY_BAUDRATE,baudRate);
  boolean parityEquals=mapContainsEqualValueByKey(properties,KEY_PARITY,parityMode);
  return baudRateEquals && parityEquals;
}
