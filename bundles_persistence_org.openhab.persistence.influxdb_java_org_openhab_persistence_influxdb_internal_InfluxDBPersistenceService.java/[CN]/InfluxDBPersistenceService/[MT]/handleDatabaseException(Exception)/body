{
  if (e instanceof RetrofitError) {
    logger.error("database connection error {}",e.getMessage());
  }
 else   if (e instanceof RuntimeException) {
    logger.error("database connection error may be wrong password, username or dbname: {}",e);
  }
}
