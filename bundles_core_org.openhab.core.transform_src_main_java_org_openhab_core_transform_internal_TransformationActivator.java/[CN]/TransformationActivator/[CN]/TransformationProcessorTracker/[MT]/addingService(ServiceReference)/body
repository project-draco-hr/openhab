{
  TransformationProcessor processor=(TransformationProcessor)context.getService(reference);
  String processorKey=extractComponentName(reference);
  processorCache.put(processorKey,processor);
  return processor;
}
