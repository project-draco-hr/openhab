{
  sourceIdentifier=xPL_Manager.getManager().getIdentifierManager().parseNamedIdentifier(vendor + "-" + device+ "."+ instance);
  logger.debug("xPL Binding source set to " + sourceIdentifier.toString());
}
