{
  for (  ItemUIProvider provider : service.getItemUIProviders()) {
    String currentIcon=provider.getIcon(itemName);
    if (currentIcon != null) {
      return currentIcon;
    }
  }
  return null;
}
