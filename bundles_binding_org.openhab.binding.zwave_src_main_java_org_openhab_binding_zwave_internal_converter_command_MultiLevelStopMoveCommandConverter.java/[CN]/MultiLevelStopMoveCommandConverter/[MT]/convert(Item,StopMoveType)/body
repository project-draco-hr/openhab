{
  int value=((PercentType)item.getStateAs(PercentType.class)).intValue();
switch (command) {
case MOVE:
    if (value == 0x00)     value=0x63;
 else     value=0x00;
  break;
case STOP:
break;
}
return value;
}
