{
  hex=hex.trim();
  if (hex != null && hex.length() > 0) {
    if (hex.startsWith("0x")) {
      hex=hex.substring(2);
    }
    int value=Integer.parseInt(hex,16);
    return value;
  }
  return 0xFF;
}
