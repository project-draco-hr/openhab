{
  String str="";
  str+=super.toString();
  str+="\n - Sub type = " + subType;
  str+="\n - Command = " + command;
  str+="\n - Dim level = " + dimmingLevel;
  str+="\n - Signal level = " + signalLevel;
  return str;
}
