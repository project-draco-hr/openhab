{
  long invalidCacheTime=System.currentTimeMillis() - CACHE_TIME;
  for (  String uuid : cachedEntries.keySet()) {
    if (cachedEntries.get(uuid).getCacheTime() <= invalidCacheTime)     cachedEntries.remove(uuid);
  }
}
