{
  if (!(item instanceof StringItem || item instanceof NumberItem)) {
    throw new BindingConfigParseException("Item '" + item.getName() + "' is of type '"+ item.getClass().getSimpleName()+ "', only StringItems and NumberItems are allowed - please check your *.items configuration");
  }
}
