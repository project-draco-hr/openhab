{
  if (!(item instanceof ColorItem)) {
    throw new BindingConfigParseException("item '" + item.getName() + "' is of type '"+ item.getClass().getSimpleName()+ "', only ColorItems are allowed - please check your *.items configuration");
  }
}
