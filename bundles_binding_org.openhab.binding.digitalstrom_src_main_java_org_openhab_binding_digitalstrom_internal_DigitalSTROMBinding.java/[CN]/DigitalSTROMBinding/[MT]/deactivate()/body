{
  for (  DigitalSTROMBindingProvider provider : providers) {
    provider.removeItemListener(this);
    provider.shutdownBinding();
    provider.removeBindingChangeListener(this);
  }
  providers.clear();
}
