{
  long invalidCacheTime=System.currentTimeMillis() - CACHE_TIME;
  for (  String uuid : cachedMessages.keySet()) {
    if (cachedMessages.get(uuid).getCacheTime() <= invalidCacheTime)     cachedMessages.remove(uuid);
  }
}
