{
  if (command instanceof IncreaseDecreaseType) {
    sendRcCommand(command,((IncreaseDecreaseType)command) == IncreaseDecreaseType.INCREASE ? RC_CHANNEL_UP : RC_CHANNEL_DOWN);
  }
 else {
    logger.error("Unsupported command type");
  }
}
