{
  try {
    String content=HttpUtils.getGetResponse(this.getHostName(),SUFFIX_CHANNEL,this.getUserName(),this.getPassword());
    return XmlUtils.getContentOfElement(content,"e2servicename");
  }
 catch (  IOException e) {
    e.printStackTrace();
    return null;
  }
}
