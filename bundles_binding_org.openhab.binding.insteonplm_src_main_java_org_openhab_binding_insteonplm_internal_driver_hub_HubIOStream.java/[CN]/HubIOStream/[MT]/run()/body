{
  while (!Thread.currentThread().isInterrupted()) {
    try {
      poll();
    }
 catch (    IOException e) {
      e.printStackTrace();
    }
    try {
      Thread.sleep(m_pollTime);
    }
 catch (    InterruptedException e) {
      break;
    }
  }
}
