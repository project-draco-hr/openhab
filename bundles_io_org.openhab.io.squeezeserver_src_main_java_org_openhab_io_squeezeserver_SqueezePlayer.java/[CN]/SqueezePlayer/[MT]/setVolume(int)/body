{
  if (this.volume != volume) {
    this.volume=volume;
    if (this.volume <= 0) {
      setMuted(true);
    }
 else {
      setMuted(false);
      this.unmuteVolume=this.volume;
    }
    fireVolumeChangeEvent();
  }
}
