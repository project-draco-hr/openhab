{
  String label=null;
  if (w.getLabel() != null) {
    label=w.getLabel();
  }
 else {
    String itemName=w.getItem();
    if (itemName != null) {
      label=getLabel(itemName);
      if (label == null)       label=itemName;
    }
  }
  return label != null ? label : "";
}
