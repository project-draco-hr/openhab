{
  this.deviceUid=deviceUid != null ? deviceUid : YamahaReceiverBinding.DEFAULT_DEVICE_UID;
  this.zone=parseZone(zone);
  this.bindingType=parseBindingType(bindingType);
  this.itemName=itemName;
}
