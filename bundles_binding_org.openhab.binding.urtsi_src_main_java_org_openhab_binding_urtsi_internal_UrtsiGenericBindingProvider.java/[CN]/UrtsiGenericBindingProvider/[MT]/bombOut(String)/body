{
  throw new BindingConfigParseException("bindingConfig '" + config + "' doesn't contain a valid Urtsii-binding-configuration. A valid configuration is matched by the RegExp '"+ CONFIG_BINDING_PATTERN.pattern()+ "'");
}
