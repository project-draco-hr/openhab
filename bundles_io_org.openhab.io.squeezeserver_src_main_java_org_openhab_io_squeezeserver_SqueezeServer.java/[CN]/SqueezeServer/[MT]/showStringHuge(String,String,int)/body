{
  SqueezePlayer player=getPlayer(playerId);
  if (player == null)   return false;
  return sendCommand(player.getMacAddress() + " show line1:" + line+ " font:huge duration:"+ String.valueOf(duration));
}
