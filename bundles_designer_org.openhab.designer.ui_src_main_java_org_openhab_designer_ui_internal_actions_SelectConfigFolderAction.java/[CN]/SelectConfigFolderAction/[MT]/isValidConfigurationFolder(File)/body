{
  if (dir.isDirectory()) {
    for (    File file : dir.listFiles()) {
      if (file.getName().equals(ConfigConstants.DEFAULT_SERVICE_CFG_FILE)) {
        return true;
      }
    }
  }
  return false;
}
