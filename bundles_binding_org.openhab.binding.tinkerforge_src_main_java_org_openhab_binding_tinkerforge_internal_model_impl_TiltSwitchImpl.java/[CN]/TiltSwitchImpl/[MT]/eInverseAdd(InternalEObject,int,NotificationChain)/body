{
switch (featureID) {
case ModelPackage.TILT_SWITCH__MBRICK:
    if (eInternalContainer() != null)     msgs=eBasicRemoveFromContainer(msgs);
  return basicSetMbrick((MBrickletTilt)otherEnd,msgs);
}
return super.eInverseAdd(otherEnd,featureID,msgs);
}
