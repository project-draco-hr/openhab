{
  if (obj == null || !(obj instanceof AstroBindingConfig)) {
    return false;
  }
  AstroBindingConfig comp=(AstroBindingConfig)obj;
  return new EqualsBuilder().append(planetName,comp.getPlanetName()).append(type,comp.getType()).append(property,comp.getProperty()).append(offset,comp.getOffset()).isEquals();
}
