{
  logger.debug("Interrupt serial connection");
  readerThread.interrupt();
  logger.debug("Close serial stream");
  try {
    out.close();
  }
 catch (  IOException e) {
  }
  logger.debug("Ready");
}
