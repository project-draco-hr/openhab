{
  return (obj != null) && (obj instanceof UrtsiItemConfiguration) && deviceId.equals(((UrtsiItemConfiguration)obj).deviceId)&& channel == ((UrtsiItemConfiguration)obj).channel && address == ((UrtsiItemConfiguration)obj).address;
}
