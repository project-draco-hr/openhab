{
  if (obj == null || !(obj instanceof WeatherBindingConfig)) {
    return false;
  }
  WeatherBindingConfig comp=(WeatherBindingConfig)obj;
  return new EqualsBuilder().append(locationId,comp.getLocationId()).append(type,comp.getType()).append(property,comp.getProperty()).append(roundingMode,comp.getRoundingMode()).append(scale,comp.getScale()).append(unit,comp.getUnit()).isEquals();
}
