{
  String str="";
  str+=super.toString();
  str+="\n - Sub type = " + subType;
  str+="\n - Id = " + sensorId;
  str+="\n - Temperature = " + temperature;
  str+="\n - Set = " + set;
  str+="\n - Mode = " + mode;
  str+="\n - Status = " + status;
  str+="\n - Signal level = " + signalLevel;
  return str;
}
