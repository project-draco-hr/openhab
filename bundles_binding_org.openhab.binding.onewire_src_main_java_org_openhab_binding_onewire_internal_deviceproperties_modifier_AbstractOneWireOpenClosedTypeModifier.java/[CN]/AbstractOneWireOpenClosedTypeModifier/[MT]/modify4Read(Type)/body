{
  if (pvType == null) {
    return null;
  }
  if (pvType instanceof OpenClosedType) {
    return modifyOpenClosedType4Read((OpenClosedType)pvType);
  }
 else {
    throw new ClassCastException("unexpected class, expected: " + OpenClosedType.class + " type is:"+ pvType.getClass());
  }
}
