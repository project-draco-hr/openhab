{
  if ((item instanceof NumberItem) || (item instanceof ContactItem) || (item instanceof StringItem)) {
    return;
  }
  throw new BindingConfigParseException("item '" + item.getName() + "' is of type '"+ item.getClass().getSimpleName()+ "', only Number, Contact, or String item types are allowed. Check your *.items configuration");
}
