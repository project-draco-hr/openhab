{
  EList<MBrickd> _mbrickds=getMbrickds();
  for (  final MBrickd mbrickd : _mbrickds) {
    if (mbrickd.getHost().equals(host) && mbrickd.getPort() == port)     return mbrickd;
  }
  return null;
}
