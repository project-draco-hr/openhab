{
  if ((port != null) && (ModbusSerialSlave.port != port)) {
    logger.debug("overriding modbus port: " + ModbusSerialSlave.port + " by: "+ port+ "but there is currently only one port supported");
  }
  ModbusSerialSlave.port=port;
}
