{
  if (connector == null) {
    connector=(serialport == null ? PrimareTCPConnector.newForModel(model,deviceId,host,port) : PrimareSerialConnector.newForModel(model,deviceId,serialport));
  }
  return connector;
}
