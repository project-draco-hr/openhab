{
  if (!(item instanceof NumberItem || item instanceof StringItem || item instanceof SwitchItem)) {
    throw new BindingConfigParseException("item '" + item.getName() + "' is of type '"+ item.getClass().getSimpleName()+ "', only String- and NumberItems are allowed - please check your *.items configuration");
  }
}
