{
  if (config != null) {
    try {
      configFileURL=new URL("file","",(String)config.get("mhtFile"));
    }
 catch (    MalformedURLException e) {
      e.printStackTrace();
    }
  }
  notifyListeners();
}
