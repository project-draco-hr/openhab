{
  if (devices == null) {
    throw new IllegalArgumentException("devices must no be null");
  }
  int counter=0;
  for (  T dev : devices) {
    if (addDevice(dev)) {
      counter++;
    }
  }
  return counter;
}
