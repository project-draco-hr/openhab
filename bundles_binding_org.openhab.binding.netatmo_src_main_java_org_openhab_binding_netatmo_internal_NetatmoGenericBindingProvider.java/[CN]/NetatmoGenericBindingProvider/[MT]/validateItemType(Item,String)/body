{
  if (!(item instanceof NumberItem || item instanceof DateTimeItem)) {
    throw new BindingConfigParseException("item '" + item.getName() + "' is of type '"+ item.getClass().getSimpleName()+ "', only NumberItems and DateTimeItems are allowed - please check your *.items configuration");
  }
}
