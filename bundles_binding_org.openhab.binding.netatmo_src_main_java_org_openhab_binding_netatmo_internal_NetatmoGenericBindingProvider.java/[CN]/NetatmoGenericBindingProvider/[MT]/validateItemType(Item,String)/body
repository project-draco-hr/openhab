{
  if (!(item instanceof NumberItem || item instanceof DateTimeItem || item instanceof CoordinateItem)) {
    throw new BindingConfigParseException("item '" + item.getName() + "' is of type '"+ item.getClass().getSimpleName()+ "', only NumberItems, DateTimeItems and CoordinateItems are allowed - please check your *.items configuration");
  }
}
