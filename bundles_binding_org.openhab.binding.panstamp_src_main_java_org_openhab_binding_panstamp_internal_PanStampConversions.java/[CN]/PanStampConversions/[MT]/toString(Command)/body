{
  if (cmd instanceof StringType) {
    return ((StringType)cmd).toString();
  }
  throw new ValueException(String.format("Unsopported command type '%s' for string endpoint",cmd.getClass().getSimpleName()));
}
