{
  Object result=getConnection(hmInterface).getAllValues();
  if (result instanceof Object[]) {
    return (Object[])result;
  }
  throw new HomematicClientException("getAllValues returns unknown result type: " + (result == null ? "null" : result.getClass()));
}
