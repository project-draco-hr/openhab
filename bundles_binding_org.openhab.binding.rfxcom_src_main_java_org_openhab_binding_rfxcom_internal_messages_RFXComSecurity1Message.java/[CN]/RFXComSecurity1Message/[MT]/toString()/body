{
  String str="";
  str+=super.toString();
  str+="\n - Sub type = " + subType;
  str+="\n - Id = " + sensorId;
  str+="\n - Status = " + status;
  str+="\n - Battery level = " + batteryLevel;
  str+="\n - Signal level = " + signalLevel;
  return str;
}
