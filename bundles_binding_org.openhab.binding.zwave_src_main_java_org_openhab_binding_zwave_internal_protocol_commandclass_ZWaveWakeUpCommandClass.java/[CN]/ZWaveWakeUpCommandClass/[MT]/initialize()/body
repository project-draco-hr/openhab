{
  ArrayList<SerialMessage> result=new ArrayList<SerialMessage>(2);
  result.add(this.getIntervalMessage());
  if (this.getVersion() > 1)   result.add(this.getIntervalCapabilitiesMessage());
  return result;
}
